<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base Theme -->
    <style name="Theme.Base" parent="Theme.Leanback">
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Launcher Theme -->
    <style name="Theme.Launcher"  parent="Theme.Base">
        <item name="android:windowBackground">@drawable/bg_poster</item>
        <item name="android:windowFullscreen">true</item>
        <item name="browseRowsMarginTop">275dp</item>
    </style>


    <!-- Browse Theme -->
    <style name="Theme.Browse" parent="Theme.Leanback.Browse">
        <item name="android:windowBackground">@color/background</item>
        <item name="defaultBrandColor">@color/colorPrimary</item>

        <item name="defaultSearchColor">@color/search_button</item>
        <!--Colores para el Search Icon -->
        <item name="defaultSearchIconColor">@color/colorPrimary</item>
        <item name="defaultSearchBrightColor">@color/md_white_1000</item>
        <item name="defaultSectionHeaderColor">@color/search_bright_color</item>
        <item name="android:windowFullscreen">true</item>
        <item name="sectionHeaderStyle">@style/SectionHeaderStyle</item>
    </style>

    <!-- Main Theme -->
    <style name="Theme.Main" parent="Theme.Leanback">
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!--Search Theme -->
    <style name="Theme.Search" parent="Theme.Leanback">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.Details" parent="Theme.Leanback.Details">
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:windowBackground">@drawable/bg_poster</item>
        <item name="android:windowFullscreen">true</item>
        <item name="detailsActionButtonStyle">@style/ActionButtonStyle</item>
    </style>

    <!--Vertical Collection -->
    <style name="Theme.VerticalGrid" parent="Theme.Leanback.VerticalGrid">
        <item name="android:windowBackground">@drawable/bg_poster</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Dialog Settings -->
    <style name="Theme.Settings" parent="Theme.Leanback">
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Leanback</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:colorPrimary">@color/background</item>
    </style>

    <style name="Theme.Wizard" parent="Theme.Leanback.GuidedStep">
        <item name="guidedStepBackground">@drawable/fondo_settings1</item>
        <item name="guidedActionsBackground">@color/transparent</item>
        <item name="guidedActionsBackgroundDark">@color/app_guidedstep_subactions_background</item>
        <item name="guidanceTitleStyle">@style/Theme.Wizard.Title</item>


    </style>

    <style name="Theme.Wizard.Title" parent="Widget.Leanback.GuidanceTitleStyle">
        <item name="android:layout_alignParentStart">true</item>
        <item name="android:layout_below">@id/guidance_breadcrumb</item>
        <item name="android:layout_centerVertical">false</item>
        <item name="android:layout_marginTop">4dp</item>
        <item name="android:fontFamily">sans-serif-light</item>
        <item name="android:gravity">start</item>
        <item name="android:lineSpacingExtra">0sp</item>
        <item name="android:lineSpacingMultiplier">1.13778</item>
        <item name="android:textAlignment">viewStart</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textSize">34sp</item>
    </style>

    <!-- ExoPlayer -->
    <!--<style name="PlayerTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
        <!--<item name="android:windowNoTitle">true</item>-->
        <!--<item name="android:windowBackground">@android:color/black</item>-->
    <!--</style>-->
    <style name="PlayerTheme" parent="Theme.Leanback">
        <!--<item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>-->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowActionBarOverlay">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

</resources>